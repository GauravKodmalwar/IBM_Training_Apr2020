# functions
# exception handling
# file handling
# csv

# reusability purpose. #, !, SPECIAL
# __name__ main
import sys
"""
def functionAddition(var1, var2, var3):
    if True:
        var1 + var2 + var3
        return True
    else:
        False

print(functionAddition(5, 10, 25))
print(functionAddition(5, "Python", 26))
print(functionAddition("Session ", "Python", " Day 2"))

temp = varText
for word in varText.split(" "):
    for keyword in varSpecial:
        if keyword in word:
            temp = temp.replace(keyword, "")
print(temp)

"""